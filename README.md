# wp-dev-template
WordPress repo template for easy start

## Quick Start

1. Install suggested VSCode extensions
2. Run `docker-compose up -d` to start the environment
3. Run debugger from VSCode (F5) to start debugging

Port mapping:
- WordPress: http://localhost:8000
- Adminer: http://localhost:8081